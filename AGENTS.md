Use the `sqlite_lab` MCP server whenever a task needs database schema context, student/course/enrollment lookup, insertion, or aggregate database analysis.

Useful resources:

- `schema://database`
- `schema://table/students`
- `schema://table/courses`
- `schema://table/enrollments`

Available tools:

- `search`
- `insert`
- `aggregate`
